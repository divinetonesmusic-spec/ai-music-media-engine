"""Schema layer — controlled vocabularies, dataclass models, and validators.

Deterministic code owns *whether output is well-formed, traceable and within V1
rules* (spec §19). Nothing here calls Claude or the network.
"""
